__pycache__/
*.pyc
*.pyo
.env
.venv/
venv/
dist/
build/
*.egg-info/
.DS_Store
Thumbs.db
config/device_library_cache.json
config/request_library_cache.json

# 本地 VS Code 配置（个人开发环境，不随仓库上传）
.vscode/

# 本地游戏调优配置（不随仓库上传，随设备/个人使用）
profiles/local/
config/active_profile.json

# 本地自签 HTTPS 证书（运行时生成，不随仓库上传）
config/certs/

# 运行时日志（不随仓库上传）
logs/

# 手机按设备保存的个人配置（不随仓库上传）
config/phone_profiles/
